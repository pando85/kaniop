# Task

Add a new optional field to an existing Kaniop custom resource and expose it in
the generated CRD and examples. Choose the affected resource by inspecting the
repository. Preserve backward compatibility and add appropriate tests.
