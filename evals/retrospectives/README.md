# Agent-system retrospectives

Create a retrospective when enough evidence has accumulated, normally monthly,
or immediately after an agent-caused incident. Generate counts from committed
evaluation results and the relevant PRs; do not ask an LLM to invent metrics.

Every retrospective must result in explicit decisions: add an eval, change a
skill, update concise repository context, add deterministic enforcement, improve
task design, or deliberately take no action.
