# Auto-generated derived type for KanidmRestoreSpec via `CustomResource`
apiVersion: kaniop.rs/v1beta1
kind: KanidmRestore
metadata:
  name: my-idm-restore
  namespace: default
spec:
  targetRef:
    name: my-idm
    uid: replace-with-kanidm-uid

  source:
    # # Local file source. Mutually exclusive with `backupRef`.
    # local:
    #   # Basename of a backup below /data/backups. Paths and traversal are rejected.
    #   fileName: backup.json.gz

  restoreImage: kanidm/server:1.10.0

  # safetyBackup:
  #   # Reference to the KanidmBackupRepository used for the pre-restore safety backup.
  #   repositoryRef:
  #     name: production-backups
  #   # Skip the mandatory safety backup. Requires break-glass annotations.
  #   skip: false
